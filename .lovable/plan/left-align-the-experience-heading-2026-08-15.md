# Left-align the Experience heading

Move the "Experience" section heading from centered to left-aligned so it reads into the postcard deck below it.

## Changes

- In the Experience section (`src/components/trail/PostcardDeck.tsx`), switch the heading wrapper from centered to left-aligned text.
- Keep the heading within the same max-width container as the postcard so its left edge lines up with the postcard's left edge.
- The tent "NEXT" button and the postcard/polaroid layout stay exactly as they are.

## Note

Other section headings (Projects, Contact) remain centered. If you'd like those left-aligned too for consistency, say so and I'll include them.
